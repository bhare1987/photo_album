function buildRows(array) {
  var row = "<div class='row'";
  array.forEach(function(el, idx){
    row += "<div class='rowItem'>"
        + "<img src='"
        + el.
    if (idx % 3 === 0) {

    }
  })
}
